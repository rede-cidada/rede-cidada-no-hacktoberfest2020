import styled from 'styled-components/macro';

export const ImageTitle = styled.img`
  object-fit: contain;
  width: 100%;
  max-width: 37rem;
  margin: 16.5rem 0 10px 0;
  transform: rotate(-4deg);
`;

export const ImageHero = styled.img`
  object-fit: contain;
  width: 100%;
  max-width: 67rem;
  margin: 10px 0;
`;
